## i clearly hate myself

# MARK: LISTS

defmodule Arrayss do
  arr = ["Luke", "Han", "Leia", "Chewbacca", "R2D2"]
  ## Loop Through an Array with a For Loop
  for item <- arr do
    IO.inspect(item)
  end
end

defmodule Listss do
end
